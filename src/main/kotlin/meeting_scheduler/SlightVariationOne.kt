package meeting_scheduler

/**
 * What if its asked that now scheduler need to schedule meeting across days
 * Well nothing would change Room class would contain Map<date, List<Meetings>>
 * instead of List<Meetings>
 */
