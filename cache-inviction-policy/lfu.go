package main

func main() {
	/*lFUCache := getLFUCache(common.Capacity)

	for _, item := range common.ItemsToBeInserted {
		lFUCache.Put(item, item)
	}

	fmt.Printf("cache hits: %d, cache miss: %d\n", cacheHitsLFU, cacheMissLFU)*/
}
